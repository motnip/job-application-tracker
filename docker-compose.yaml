version: '3'
services:
  database-source:
    image: "postgres:17.4-alpine"
    container_name: "job-application-test-db"
    environment:
      - POSTGRES_USER=jobapp-admin
      - POSTGRES_PASSWORD=tomas0584!
      - POSTGRES_DB=jobapp-db
    ports:
      - "5432:5432"
    #Al momento il volume lo possiamo lasciar pedere. Poi ce ne occuperemo dopo.
    #volumes:
    #  - ./init-database:/docker-entrypoint-initdb.d